<Window x:Class="Assessment_1.AddAnimalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Animal" Height="217" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Name:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtName" Grid.Row="0" Margin="100,0,0,5" Width="200"/>

        <TextBlock Text="Type:" Grid.Row="1" VerticalAlignment="Center"/>
        <ComboBox x:Name="cbAnimalType" Grid.Row="1" Margin="100,0,0,5" Width="200" />

        <TextBlock Text="Sex:" Grid.Row="2" VerticalAlignment="Center"/>
        <ComboBox x:Name="cbSex" Grid.Row="2" Margin="100,0,0,5" Width="200" />

        <TextBlock Text="Age:" Grid.Row="3" VerticalAlignment="Center"/>
        <TextBox x:Name="txtAge" Grid.Row="3" Margin="100,0,0,5" Width="200"/>

        <TextBlock Text="Owner Name:" Grid.Row="4" VerticalAlignment="Center"/>
        <TextBox x:Name="txtOwnerName" Grid.Row="4" Margin="100,0,0,5" Width="200"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Width="80" Margin="0,0,10,0" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
